# Essentiality Gate

Before adding code, an abstraction or a dependency, record evidence for all four checks:

1. an existing project implementation;
2. the language standard library;
3. a native platform or framework capability;
4. an already installed dependency.

Reuse a viable option. Add only the minimum change when none is viable. The gate cannot remove safety, accessibility, compatibility or mandatory verification requirements.
