---
description: Search, explain or remove reusable project knowledge
argument-hint: "<search|explain|remove> [arguments]"
---

Handle project memory with arguments: $ARGUMENTS

`search <text>`: run the `cycle_memory` tool with a search operation
(limit 10) and report results with their confidence level (inferred,
user_asserted, verified). `explain <id>` and `remove <id>` (ask the user
to confirm removal first). Unknown subcommand: list the three and stop.
