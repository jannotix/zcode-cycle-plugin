---
description: Show managed browser QA status and limits
---

Report the managed browser QA facts: isolated temporary profile, loopback
applications allowed by default, external origins require explicit native
approval, bounded concurrent sessions, evidence recorded outside the
repository and bound to the candidate digest, interactive actions only for
the executor role. State that browser operations are tool-driven during a
governed workflow; there is no separate interface.
