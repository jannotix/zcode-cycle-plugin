---
description: Retry a classified failure or blocked cycle
---

Retry via the `cycle_control` tool (operation `retry`) for this project and
report what the control plane classified and restarted. If nothing is
retryable, report the daemon's reason without guessing.
