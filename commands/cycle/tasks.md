---
description: Show durable task identifiers and states
---

Report durable workflow tasks via the `cycle_control` tool (operation
`tasks`) for this project: task identifiers, titles and states, exactly as
the control plane returned them.
