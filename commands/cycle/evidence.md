---
description: Show recorded candidate gates without raw command output
---

Report verification evidence via the `cycle_control` tool (operation
`evidence`) for this project: gate kind, invocation, status and skip
reasons. Never paste raw command output; the control plane's summarized
records are the report.
