---
description: Run or control ZCode Cycle (default shows status and available commands)
argument-hint: "[command] [arguments]"
---

Handle the ZCode Cycle request: $ARGUMENTS

If no command was given: report the current workflow status via the
`cycle_control` tool (operation `status`) for this project, then list the
available commands exactly as documented in the ZCode Cycle command
reference. Do not call any other tool.
