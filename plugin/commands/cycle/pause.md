---
description: Pause the latest workflow at its next safe boundary
---

Pause the active workflow via the `cycle_control` tool (operation `pause`)
for this project and report the pause point the control plane returned.
Paused work keeps its state and resumes with /cycle:resume.
